package com.example.demo.accno;

import java.util.Random;

import lombok.Getter;
import lombok.Setter;

@Setter
@Getter
public class Accno {
private String accno;





/*
 * Random r=new Random(); public String generate() { int a=randomnum(); String
 * a1=Integer.toString(a); int b=randomnum(); String b1=Integer.toString(b); int
 * c=randomnum(); String c1=Integer.toString(c); int d=randomnum(); String
 * d1=Integer.toString(d); return a1+b1+c1+d1;
 * 
 * } public int randomnum() { return (int)(Math.random() * (9999 - 1000 + 1) +
 * 1000); }
 */
}
